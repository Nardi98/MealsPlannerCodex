name: CI

on:
  push:
  pull_request:

jobs:
  backend:
    runs-on: ubuntu-latest
    env:
      DATABASE_URL: sqlite+pysqlite:///tmp/mealplanner_ci.db
      TEST_DATABASE_URL: sqlite+pysqlite:///tmp/mealplanner_ci.db
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - run: pip install -r requirements.txt
      - run: flake8 .
      - run: alembic upgrade head
      - run: pytest

