[flake8]
max-line-length = 120
exclude =
    pages,
    tests,
    # Alembic revision scripts are machine-generated by `alembic revision
    # --autogenerate` and reformatting them by hand would be undone by the next
    # one. migrations/env.py is hand-written and stays linted.
    migrations/versions
